[project]
name = "blf-example-map-reduce"
version = "0.1.0"
description = "Resume screening: map an LLM evaluator over N resumes in parallel, then reduce the verdicts to summary stats."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "PyYAML>=6.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
