[project]
name = "blf-example-agent"
version = "0.1.0"
description = "Research agent: Decide -> Search -> (loop) -> Answer."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
