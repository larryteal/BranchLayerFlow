[project]
name = "blf-example-majority-vote"
version = "0.1.0"
description = "Ask 5 LLM workers the same hard question, then take a majority vote on the final answer."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "anthropic>=0.40",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
