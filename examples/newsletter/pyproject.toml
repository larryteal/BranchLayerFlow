[project]
name = "blf-example-newsletter"
version = "0.1.0"
description = "AI newsletter pipeline: curate, filter, summarise, format markdown."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "duckduckgo-search>=6.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
