[project]
name = "blf-example-streamlit-fsm"
version = "0.1.0"
description = "Streamlit image-gen FSM: prompt -> generate -> approve | regenerate -> done."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "streamlit>=1.36",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
