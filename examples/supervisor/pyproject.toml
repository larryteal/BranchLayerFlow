[project]
name = "blf-example-supervisor"
version = "0.1.0"
description = "Inner research agent (sometimes unreliable) wrapped by a supervisor that retries on bad answers."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
