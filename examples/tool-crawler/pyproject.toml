[project]
name = "blf-example-tool-crawler"
version = "0.1.0"
description = "Domain-bounded crawler + LLM summariser pipeline."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "requests>=2.31",
    "beautifulsoup4>=4.12",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
