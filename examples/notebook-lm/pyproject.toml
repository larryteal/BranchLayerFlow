[project]
name = "blf-example-notebook-lm"
version = "0.1.0"
description = "Document -> dialogue -> two-voice TTS podcast (mp3)."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "pydub>=0.25",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
