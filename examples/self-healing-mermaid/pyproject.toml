[project]
name = "blf-example-self-healing-mermaid"
version = "0.1.0"
description = "NL -> Mermaid diagram with compile-and-fix retry loop using mmdc."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
