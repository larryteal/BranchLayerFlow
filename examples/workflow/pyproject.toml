[project]
name = "blf-example-workflow"
version = "0.1.0"
description = "Three-stage writing pipeline: outline -> draft -> style."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "PyYAML>=6.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
