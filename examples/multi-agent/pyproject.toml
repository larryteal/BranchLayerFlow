[project]
name = "blf-example-multi-agent"
version = "0.1.0"
description = "Two-agent Taboo game: Hinter gives clues avoiding forbidden words, Guesser tries to identify the target."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
