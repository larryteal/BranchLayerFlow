[project]
name = "blf-example-thinking"
version = "0.1.0"
description = "Chain-of-thought reasoning loop: a single agent iterates on a hard problem until it commits to an answer."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "anthropic>=0.40",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
