[project]
name = "blf-example-parallel-batch"
version = "0.1.0"
description = "Parallel translation of one document into 8 languages via AsyncParallelBaseFlow."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "anthropic>=0.40",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
