[project]
name = "blf-example-tool-embeddings"
version = "0.1.0"
description = "OpenAI text embeddings as a tool, wired through a BLF agent."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "numpy>=1.20",
    "python-dotenv>=1.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
