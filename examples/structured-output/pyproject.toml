[project]
name = "blf-example-structured-output"
version = "0.1.0"
description = "Resume parser: extract YAML-structured fields from a free-text resume."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "branchlayerflow",
    "openai>=1.0",
    "PyYAML>=6.0",
]

[tool.uv.sources]
branchlayerflow = { workspace = true }
